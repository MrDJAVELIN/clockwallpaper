import React from "react";
import { Clock } from "./Clock";

export const App = () => {
    return (
        <>
            <div className="clock-container">
                <Clock />
            </div>
        </>
    );
};
